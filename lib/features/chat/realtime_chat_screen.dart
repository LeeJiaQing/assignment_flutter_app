//jason
import 'package:flutter/material.dart';

class RealtimeChatScreen extends StatelessWidget {
  const RealtimeChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Realtime Chat Page'),
    );
  }
}
